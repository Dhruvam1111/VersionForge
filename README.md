# VersionForge
It is a mini replica of git version control system, build locally to manage files and directories on OS for multiple users. It gives options to initialise a directory as a repo, commit to a repo, create multiple branches, merge repos, fork repo, push, pull etc, on our system.
